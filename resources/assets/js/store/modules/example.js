import * as types from '../mutation-types'

// initial state

const state = {
	header: 'Laravel Vue'
}

// getters
const getters = {
  
}

// actions
const actions = {
 
}

// mutations
const mutations = {
  
}

export default {
  state,
  getters,
  actions,
  mutations
}